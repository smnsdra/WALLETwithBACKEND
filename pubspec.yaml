name: simple_wallet
description: A minimal wallet app (income/expense/saved/cash/debt) — no external packages.
publish_to: "none"
version: 0.0.1

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true